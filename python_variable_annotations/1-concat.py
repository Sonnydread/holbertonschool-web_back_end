#!/usr/bin/env python3
"""takes 2 strings and returns a concatenated string"""


def concat(str1: str, str2: str) -> str:
    """takes 2 strings and returns a concatenated string"""
    return str1 + str2
